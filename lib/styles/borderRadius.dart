import 'package:flutter/material.dart';

class AppBorderRadius {
  static BorderRadius noradius = BorderRadius.circular(0);
  static BorderRadius small = BorderRadius.circular(4);
  static BorderRadius regular = BorderRadius.circular(8);
  static BorderRadius large = BorderRadius.circular(16);
}
